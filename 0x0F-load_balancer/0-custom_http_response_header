#!/usr/bin/env bash
#Double the number of webservers

sudo apt-get update
sudo apt-get install -y nginx

hostname=$hostname

echo "add_header $hostname;" | sudo tee -a /etc/nginx/nginx.conf

sudo systemctl restart nginx
